# 🎯 CONFIGURATION POUR LES TESTS AUTOMATIQUES

# Base de données H2 en mémoire (comme un frigo temporaire)
spring.datasource.url=jdbc:h2:mem:testdb

# Utilisateur et mot de passe simples
spring.datasource.username=sa
spring.datasource.password=

# Driver H2
spring.datasource.driver-class-name=org.h2.Driver

# Créer automatiquement les tables pour les tests
spring.jpa.hibernate.ddl-auto=create-drop

# Dialect H2
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Désactiver la console H2 (pas besoin pour les tests)
spring.h2.console.enabled=false
